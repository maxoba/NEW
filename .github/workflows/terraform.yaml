name: terraform-build

on:
  push:
    branches:
      - main
      
jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo "Running terraform"
      - name: checkout
        uses: actions/checkout@v4
      - name: check  
        run: ls                